{% extends 'hoteles/baseHotel.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block contenido %}

<h2>Listado de Hoteles </h2>
<select class="selectProv">
    <option value="">Seleccione provincia...</option>
    {% for hotel in hoteles %}
        <option placeholder="Seleccione provincia" value="">{{ hotel.provincia }}</option>
    {% endfor %}
</select>
    <div class="row row-cols-1 row-cols-md-2">
        {% for hotel in hoteles %}
            <div class="col mb-4">
                <div class="card  myCard" style="width: 18rem;">
                    <img width='320px' height='213px' src="{{ hotel.imagen }}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ hotel.nombre }}</h5>
                        <p class="card-text">{{ hotel.precio }}€</p>
                        <a href="{{ path('detalleHotel', {id:hotel.id}) }}" class="btn btn-primary dark">Más Info</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}